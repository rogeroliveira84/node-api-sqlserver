'use strict';

class Client {
  constructor (client) {
    this.id = client.id;
    this.clientName = client.name;
    this.birthDate = client.groupId;
    this.createdOn = client.createdOn;
    this.state = client.status;
  }
}

exports.Client;